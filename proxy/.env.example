# --- Sitecore marketer-mcp OAuth ---
# Run: npm run auth
# Auth mode is auto-detected:
#   - If SITECORE_CLIENT_SECRET is set → client_credentials (headless, no browser)
#   - If SITECORE_CLIENT_SECRET is empty → Authorization Code + PKCE (browser login)

# Sitecore MCP endpoint (default is production marketer-mcp)
SITECORE_MCP_URL=https://edge-platform.sitecorecloud.io/mcp/marketer-mcp-prod

# Sitecore Identity OAuth endpoints
SITECORE_AUTH_URL=https://auth.sitecorecloud.io/authorize
SITECORE_TOKEN_URL=https://auth.sitecorecloud.io/oauth/token

# OAuth client ID — from Sitecore Cloud Portal > Credentials > Automation
SITECORE_CLIENT_ID=

# OAuth client secret — Automation credentials have this; leave empty for PKCE flow
SITECORE_CLIENT_SECRET=

# Sitecore API audience
SITECORE_AUDIENCE=https://api.sitecorecloud.io

# --- Proxy config ---
PORT=3001

# Path to persisted tokens (Railway volume mount)
TOKEN_PATH=/data/tokens.json

# Proxy auth token — required by OpenClaw plugin to call tool endpoints
# Generate with: openssl rand -hex 32
# If empty, tool endpoints are unauthenticated (dev mode only)
MCP_PROXY_TOKEN=
